-- ── Tema visual ──────────────────────────────
return {
    active_border   = "rgba(7aa2f7ff)", -- color borde ventana activa (formato RRGGBBAA)
    inactive_border = "rgba(353d57ff)", -- color borde ventanas inactivas
    gaps_in         = 5,   -- hueco entre ventanas (px)
    gaps_out        = 10,  -- hueco entre ventanas y pantalla (px)
    border_size     = 2,   -- grosor del borde (px)
    rounding        = 8,   -- radio de esquinas (px); 0 para esquinas rectas
}
